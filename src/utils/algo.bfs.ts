import { IGraph } from "./util.state";

export default class BFS{
    graph: IGraph
    
    /**
     *
     */
    constructor(graph: IGraph) {
        this.graph = graph;
    }

    run(){
        alert("todo ")
    }
}