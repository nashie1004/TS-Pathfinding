import { IGraph } from "./util.state";

export default class DFS{
    graph: IGraph

    /**
     *
     */
    constructor(graph: IGraph) {
        this.graph = graph;
    }

    run(){
        alert("todo ")
        //
    }
}